from yaml import dump


dictionary = {
    'networks':
    [

    ]
}

def create_yaml(dictionary: dict) -> dict:
    while True:
        number_of_networks = int(input("How many networks you want? "))
        for i in number_of_networks:
            network_name = input("Enter the network name: ")
            network_subnet = input("Enter the subnet: ")
            dmz = bool(input("Is it in DMZ, True or False"))
            dictionary['netwokrs'][0] = {network_}
        